version: '3.8'

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: random-roll-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - RUST_LOG=info
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge
